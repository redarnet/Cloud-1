acceder au worpres par vagrant > en localhost 
http://192.168.56.10:8080

db ?
http://192.168.56.10:8081  ou 3306 ?


# Lancer directement le playbook
ansible-playbook -i inventory/local.ini playbook.yml

# Se connecter au conteneur MySQL
docker exec -it wordpress_db mysql -u wordpress -p
# Entrer le mot de passe défini dans votre .env
# (celui de MYSQL_PASSWORD)

USE wordpress;
SHOW TABLES;
SELECT * FROM wp_users;


Clean docker
# Reset complet de WordPress (efface tout)
cd /opt/wordpress && sudo docker-compose down -v && docker-compose up -d

# Simple redémarrage (garde les données)
cd /opt/wordpress && docker-compose restart

# Arrêt
cd /opt/wordpress && docker-compose down


 cd .. && sudo rm -rf wordpress/ && cd  /vagrant/cloud


apt update && apt upgrade -y


apt install -y \
  ca-certificates \
  curl \
  gnupg \
  lsb-release \
  git \
  sudo

apt update && apt upgrade -y


ssh root@163.172.162.141
ssh-keygen -t ed25519 -C "scaleway-deploy" -f ~/.ssh/id_ed25519_github

ansible-galaxy collection install community.docker
